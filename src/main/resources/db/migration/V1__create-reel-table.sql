create table reel (
  id varchar(255),
  recipient_id varchar(255),
  condition varchar(255),
  widget_config_id varchar(255),
  accumulated_amount varchar(255),
  required_amount varchar(255),
  items text
);
